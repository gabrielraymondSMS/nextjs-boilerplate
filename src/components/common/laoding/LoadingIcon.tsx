import React from "react";
import MyIcon from "../icon/MyIcon";

const LoadingIcon = () => {
  return (
      <MyIcon src="/assets/icons/ic-loading.svg" className="animate-spin" width={20} height={20} />
  );
};

export default LoadingIcon;
